
<%= form_for new_collection_element_path(@newElement, {:collection_id => @newElement.collection_id})  do |f| %>

<%= f.label "Element Name" %>
<%= f.text_field :name %>
<!-- % :collection_id = @element.collection_id % -->
<!--  %= f.inspect % -->
<br>


<br>
<%= f.submit %>

<% end %> 